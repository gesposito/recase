import React, { Component } from "react";
import { render } from "react-dom";

class Index extends Component {
  render() {
    return <section>Recase</section>;
  }
}

render(<Index />, document.getElementById("app"));
